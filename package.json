{
  "name": "tessa-matrix-studio",
  "version": "1.9.2",
  "private": true,
  "description": "TESSA Matrix Studio userscript verification commands",
  "scripts": {
    "test": "node --check tessa-matrix-studio.user.js && node tests/smoke.mjs && node tests/planner.mjs && node tests/docs.mjs"
  }
}
