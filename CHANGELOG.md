# Changelog

## 1.9.24 — 2026-08-28

- добавлена fail-closed структурная проверка SpreadsheetML после ZIP/OPC guard и до построения workbook/Preview;
- максимальный используемый номер строки ограничен 100 000, столбцы — штатным пределом Excel XFD / 16 384; лимит применяется и к неявно нумеруемым строкам без атрибута `r`;
- физически разбираемые XML-узлы ограничены 100 000 строками и 500 000 ячейками на лист, чтобы компактный патологический XML не мог разогнать CPU/RAM; пропущенные номера строк остаются sparse-gap и не материализуются тысячами пустых массивов;
- отклоняются дубли номеров строк и координат ячеек, нулевые/повреждённые references, координаты за XFD и несовпадение номера строки ячейки с родительским <row>;
- regression suite покрывает row ceiling, implicit-row overflow, sparse-gap materialization, XFE, excessive rows/cells, duplicate coordinates, row/cell mismatch и malformed zero references; обычный namespaced SpreadsheetML и Roundtrip V6 сохраняют совместимость.

## 1.9.23 — 2026-08-28

- XLSX теперь проходит fail-closed проверку ZIP/OPC-пакета до XML-парсинга: опасный или повреждённый архив не доходит до Preview и Apply;
- добавлены ресурсные пределы: 32 МБ на исходный XLSX, 256 ZIP-частей, 128 МБ на одну распакованную часть, 256 МБ суммарно и степень сжатия не более 100×;
- отклоняются ZIP64, шифрованные entries, небезопасные и дублирующиеся пути, многотомные архивы, неподдерживаемые методы сжатия и выходящие за границы ZIP-смещения;
- DEFLATE распаковывается потоково с контролем фактически полученных байтов и фактической степени сжатия, поэтому ложный declared size не обходит лимиты;
- synthetic security-regressions покрывают oversized input, ZIP bomb, ZIP64/encryption, path traversal, duplicate paths и повреждённые offsets; обычный Roundtrip V6 остаётся совместимым.

## 1.9.22 — 2026-08-28

- `StateName` матрицы теперь проходит штатную локализацию TESSA до отображения и safety-проверки: непрозрачные ключи `$Mtx_Enums_*` больше не превращают известное состояние в «Неизвестное состояние»;
- caption и writable-guard используют единый нормализованный state: `Черновик` остаётся доступен для изменений, `Активная` и `Согласование` остаются read-only;
- stale-проверка обычных строк, REPLACE-source и copied-ADD-source различает реальное изменение данных и display-only переименование при неизменном стабильном ID исполнителя/справочного значения;
- destructive DELETE намеренно остаётся на строгом raw `BaseFingerprint`: display-only послабление не может разрешить удаление устаревшей строки;
- REPLACE после Preview контролирует source уже от свежего raw fingerprint, сохраняя TOCTOU-защиту между Preview и Apply;
- добавлены regression-сценарии для opaque localization keys и реального кейса «тот же role ID, другое отображаемое имя → NOOP», а release-документация и version-контракты синхронизированы с 1.9.22.

## 1.9.21 — 2026-08-28

- исправлена safety-зависимость UPDATE/ADD → DELETE для типизированных значений, которые семантически равны, но по-разному отображаются в Excel и TESSA;
- Boolean `Да` / `true`, а также Int, Decimal, Date и DateTime теперь нормализуются только при сопоставлении зависимого DELETE;
- основной `fingerprintFlat` намеренно не изменён: stale/integrity-проверки продолжают использовать точный исходный fingerprint;
- добавлен regression из реального UAT: если UPDATE в состояние удаляемой строки отклонён как duplicate, связанный DELETE также обязан перейти в ПРОПУСТИТЬ.

## 1.9.20 — 2026-08-28

- Preview теперь выполняет read-only preflight до отображения итогового плана и не считает заведомо неприменимые операции корректными;
- несуществующие значения справочников, duplicate/stale runtime-конфликты и другие preflight-ошибки сразу переводятся в ПРОПУСТИТЬ с причиной в Preview;
- зависимый DELETE также исключается из Apply, если связанный UPDATE/ADD уже не прошёл Preview-preflight;
- Preview-preflight перечитывает свежий snapshot TESSA вместо reuse session-cache; перед фактической записью Apply повторяет preflight ещё раз;
- planner-plan не мутируется при проекции runtime SKIP в пользовательский Preview; добавлен regression `preview-preflight.mjs`.

## 1.9.19 — 2026-08-28

- исправлена проверка зависимого DELETE при неполной структуре: если в строке отсутствуют ожидаемые столбцы, DELETE блокируется fail-closed;
- добавлен regression для stale-schema сценария с отсутствующими колонками.

## 1.9.18 — 2026-08-28

- зависимый DELETE теперь повторно проверяется перед записью: если связанный UPDATE/ADD уже стал невозможен, удаление не выполняется отдельно;
- добавлена предварительная проверка зависимых DELETE перед Apply.

## 1.9.17 — 2026-08-28

- copied ADD повторно проверяет исходную строку по свежему fingerprint перед Apply;
- если source изменилась после Preview, копирование блокируется как stale.

## 1.9.16 — 2026-08-28

- REPLACE-source повторно проверяется по свежему fingerprint перед Apply;
- если source изменилась после Preview, замена блокируется как stale.

## 1.9.15 — 2026-08-28

- удалён встроенный генератор QA-файлов из production userscript;
- тестовые генераторы остаются только в repository test tooling.

## 1.9.14 — 2026-08-28

- обновления Tampermonkey переведены на GitHub Releases: `@updateURL` указывает на metadata-файл, `@downloadURL` — на userscript;
- release-CI проверяет публичные latest-ссылки и SHA-256.

## 1.9.13 — 2026-08-28

- исправлена установка/обновление через `releases/latest` и Tampermonkey.

## 1.9.12 — 2026-08-28

- добавлен выборочный review diff с возможностью не применять отдельные изменения/строки и вернуть их обратно до Apply.

## 1.9.11 — 2026-08-28

- schema-refresh теперь fail-closed при stale исходной строке и не переносит устаревшие изменения на свежую структуру.

## 1.9.10 — 2026-08-28

- усилена защита DELETE/overwrite при неоднозначной позиции: Studio не удаляет строку, если нельзя доказать безопасную связь с изменением.

## 1.9.9 — 2026-08-28

- Preview учитывает сортировку и корректно различает overwrite после перестановки строк.

## 1.9.8 — 2026-08-28

- overwrite-source защищён от stale изменений по fingerprint.

## 1.9.7 — 2026-08-28

- schema-refresh учитывает sorted overwrite и сохраняет правильную identity строки.

## 1.9.6 — 2026-08-28

- исправлено сопоставление overwrite после сортировки Excel.

## 1.9.5 — 2026-08-28

- синхронизирована внутренняя версия приложения с userscript metadata.

## 1.9.4 — 2026-08-28

- усилен release integrity pipeline и проверка артефактов.

## 1.9.3 — 2026-08-28

- зафиксирован production acceptance flow и обновлена документация реального preview.

## 1.9.2 — 2026-08-28

- исправлена установка userscript через release asset.

## 1.9.1 — 2026-08-28

- первая production-линия TESSA Matrix Studio с roundtrip Excel → Preview → Apply.
