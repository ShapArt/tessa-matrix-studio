# Warehouse v14 RED gate

This branch intentionally starts with failing production-bundle, kernel, and browser-runtime contracts. The first green commit must make the exact UAT/release userscript contain Warehouse v14 rather than only making isolated source tests pass.
