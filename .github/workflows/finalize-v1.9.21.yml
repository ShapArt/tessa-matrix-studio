name: Temporary v1.9.21 finalizer

on:
  push:
    branches:
      - fix/typed-dependent-delete-v1.9.21
    paths:
      - scripts/finalize-v1.9.21.py

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/typed-dependent-delete-v1.9.21
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false
      - name: Apply release metadata
        run: python3 scripts/finalize-v1.9.21.py
      - name: Full regression suite
        run: npm test
      - name: Diff hygiene
        run: git diff --check
      - name: Commit release state and remove helper
        run: |
          rm -f scripts/finalize-v1.9.21.py .github/workflows/finalize-v1.9.21.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: finalize v1.9.21 release metadata"
          git push origin HEAD:fix/typed-dependent-delete-v1.9.21
