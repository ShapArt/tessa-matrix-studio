name: Dev Patch v1.9.25

on:
  push:
    branches:
      - fix/excel-auto-date-v1.9.25

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: fix/excel-auto-date-v1.9.25
          fetch-depth: 1
      - name: Patch style-aware XLSX parsing and Text export
        run: python .github/scripts/patch-v1.9.25.py
      - name: Verify export regression GREEN
        run: node tests/xlsx-auto-date.mjs
      - name: Commit patch and remove helpers
        run: |
          rm .github/workflows/dev-patch-v1.9.25.yml .github/scripts/patch-v1.9.25.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tessa-matrix-studio.user.js .github/workflows/dev-patch-v1.9.25.yml .github/scripts/patch-v1.9.25.py
          git commit -m "fix: preserve Excel text cell format metadata"
          git push origin HEAD:fix/excel-auto-date-v1.9.25
