name: Quality & Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '17 3 * * 1'

# Default token is read-only. Jobs that truly need additional scopes declare them locally.
permissions:
  contents: read

jobs:
  dev_patch_v1928:
    name: Temporary v1.9.28 patch helper
    if: github.event_name == 'pull_request' && github.head_ref == 'fix/v1.9.28-targeted-delete-recheck'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Patch production source
        run: python3 tools/dev-v1928-patch.py
      - name: Commit patch to feature branch
        run: |
          if git diff --quiet -- tessa-matrix-studio.user.js; then
            echo 'No source change required'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tessa-matrix-studio.user.js
          git commit -m 'fix: target DELETE freshness checks'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}

  tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      # External Actions are pinned to full commit SHAs. Dependabot proposes reviewed SHA updates.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          package-manager-cache: false
      - name: Syntax and regression tests
        run: npm test

  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    # SARIF upload needs security-events: write; keep that capability out of the test job.
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          languages: javascript-typescript
      - uses: github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
