name: Dev Picker DOM GREEN

on:
  push:
    branches:
      - feat/dictionary-picker-hardening

permissions:
  contents: write

jobs:
  green:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: feat/dictionary-picker-hardening
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 24
          package-manager-cache: false
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Apply picker DOM patch
        run: node tools/dev-picker-dom-patch.mjs
      - name: Targeted picker DOM and core contracts
        run: |
          node tests/picker-production-ux.mjs
          node tests/picker-dom-production.cjs
          node tests/ui-dom.cjs
          node tests/record-keeping-partners.mjs
          node tests/picker-performance.mjs
          node tests/schema-refresh-circular-regression.mjs
      - name: Full regression
        run: npm test
      - name: Commit verified picker UI and remove scaffolding
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/dev-picker-dom-red.yml .github/workflows/dev-picker-dom-green.yml tools/dev-picker-dom-patch.mjs
          git add tessa-matrix-studio.user.js package.json tests/picker-dom-production.cjs
          git commit -m "feat: ship production multi-value picker UX"
          git push origin HEAD:feat/dictionary-picker-hardening
