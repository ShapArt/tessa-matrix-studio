name: Patch v1.9.37 consume stale plan
on:
  push:
    branches: [feat/preprod-mega-uat-hardening]
    paths:
      - scripts/patch_v1937_consume_plan.py
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: feat/preprod-mega-uat-hardening
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 24
      - name: Apply deterministic patch
        run: python scripts/patch_v1937_consume_plan.py
      - name: Focused tests
        run: |
          node tests/apply-refresh-failure-result.mjs
          node tests/apply-report-opt-in.mjs
          node tests/apply-plan-consumed.mjs
      - name: Commit source change and self-remove helper
        run: |
          rm scripts/patch_v1937_consume_plan.py .github/workflows/patch-v1937-consume-plan.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "fix: consume stale Apply plan after writes"
          git push origin HEAD:feat/preprod-mega-uat-hardening
